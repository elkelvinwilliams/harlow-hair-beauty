# 👋 Start here (handoff to remote/iPad session)

You are continuing an in-progress project. The desktop session set everything up;
your job is to build the site. Read this, then read `README.md`.

## What this is
A premium, multi-page **hair & beauty salon website** for a salon in **Harlow, England**,
built around **taking bookings**. Stack: **HTML + Tailwind CSS**. Style: **elegant & premium**.
Scope: **multi-page (4–6 pages)**.

## What's done
- Git repo created and pushed to GitHub (private): `elkelvinwilliams/harlow-hair-beauty`
- Project decisions + content checklist documented in `README.md`
- Scaffold only — **no site pages built yet**

## Where to pick up: the content checklist
The client (Elkelvin) is supplying **real brand + content**, to be gathered **one item at a time**.
Work through the checklist in `README.md` in order, starting at **Item 1: Salon name + tagline**.
Ask for one item, wait for the answer, then move to the next. Don't dump all 12 questions at once.

Planned pages: `index.html` (Home), `services.html`, `about.html`, `gallery.html`, `contact.html`.

## Build standards (the "$10k" bar)
- Elegant, premium feel; generous whitespace; refined serif/sans pairing.
- Tasteful, performant animations — use `transform`/`opacity` only; respect `prefers-reduced-motion`.
- Fully responsive (375 / 768 / 1024 / 1440); no horizontal scroll on mobile.
- Accessibility: 4.5:1 contrast, visible focus states, alt text, labelled inputs, 44px touch targets.
- SVG icons (not emojis). Real, correct brand logos for social links.
- "Book" CTA is prominent on every page (method TBC in checklist item 12).

## Workflow with the other (desktop) session
This repo is the single source of truth. After meaningful changes: **commit and push**
so the desktop session can pull. Likewise, `git pull` before starting to get the latest.

## First message to send
Greet Elkelvin and ask **checklist Item 1: the salon name + tagline.**
